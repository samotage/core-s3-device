// WiFi credentials for the meeting recorder's HTTP server.
//
// Copy this file to `include/secrets.h` and fill in your network.
// `secrets.h` is gitignored and must NEVER be committed (this repo is public).
//
// If secrets.h is absent the firmware still builds — it just runs the recorder
// offline (no WiFi, no HTTP server).

#ifndef __SECRETS_H
#define __SECRETS_H

#define WIFI_SSID    "your-wifi-ssid"
#define WIFI_PASS    "your-wifi-password"

// mDNS hostname — the device is reachable at http://<REC_HOSTNAME>.local
#define REC_HOSTNAME "core-s3"

// Headspace notify endpoint (plain HTTP, LAN — the ESP32 can't do TLS/Tailscale).
// The device POSTs a small JSON ping here when it has un-uploaded recordings;
// Headspace then pulls them from the device's /rec/ endpoint. Leave undefined
// to disable notifications (recorder still works offline).
#define HEADSPACE_NOTIFY_URL "http://192.168.4.11:15055/api/recorder/notify"

#endif  // __SECRETS_H
